<div class="wood"></div>

<% unless @avatar %>
<div id="welcome_container" class="container">
		<% @avatars.each do |avatar| %>
			<a class="entry_link" href="<%=request.path%>?avatar_id=<%= avatar.id %>&place_id=<%= avatar.place_id %><% if params[:sound_on] %>&sound_on=1<% end %>"><%= avatar.name %> in <%= avatar.place.name %></a><br/>
		<% end %>
</div>
<% end %>

<div id="chat_container" class="container">

	<h3>Flüstern an der Theke</h3>
<% if @avatar %>
	<div id="suggestions_new"></div>	
	<form id="suggestion_input_form">
		<span class="suggestion_label"><%= @user_name %></span>
		<input id="suggestion_input" type="text" maxlength="79"/>
		<input id="suggestion_submit" type="submit" value="Flüstern"/>
		<input id="chat_name" type="hidden" size="10" value="<%= @user_name %>"/>
	</form>
<% end %>

</div>

<div id="avatar_container" class="container">
	<h3>Ansagen für den Barvatar</h3>
	<% if @avatar %>
	<div id="suggestions_top"></div>
	<% end %>
</div>
